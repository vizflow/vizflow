document.imageList    = ['./image/frameratetest.png'] ; // configure static assets for the preloader
document.audioList    = [] ; // optional static assets

document.testConfig = {
    nCircle: [50, 100, 200, 400, 800],
    duration: 5,
    bgColor: '#FFF',
    // bgColor: '#D5DADC',
    vizSize: 1200,
    maxSpeed: 10,
    minSpeed: 5,
    maxSize: 50,
    minSize: 5,
};

document.results = {
    min: [],
    max: []
};